// Compatibility re-export barrel — types are now defined in per-domain files.
// Import from src/domain/index.ts (or specific sub-files) for new code.
// This file will be removed once all direct imports have migrated.
export * from './index'
