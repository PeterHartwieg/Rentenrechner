> *This was generated by AI during triage.*

title: "Als Plan speichern button appears to do nothing"
Status: done
Severity: P1
Category: bug
Type: AFK
Area: recommender / plan saving / what-if adoption

## What to build

Investigate and fix the `Als Plan speichern` button in the new flow. It currently appears to do nothing when clicked.

The expected behavior is that the user can save or adopt the proposed plan in a visible, durable way without silently losing their work.

## Repro

1. Open the new `Lücke schließen` or recommender flow.
2. Progress to a result where `Als Plan speichern` is visible.
3. Click `Als Plan speichern`.
4. Observe that there is no apparent state change, confirmation, navigation, or persisted plan.

## Acceptance criteria

- [ ] Clicking `Als Plan speichern` performs a clear action: saves/adopts the selected plan, updates visible state, and gives the user feedback.
- [ ] The saved plan persists across reload if persistence is intended for this flow.
- [ ] The action does not mutate the baseline before the explicit click.
- [ ] If the selected result should become a what-if scenario rather than baseline, the UI makes that clear.
- [ ] A regression test clicks the button and asserts the expected state change.

## Implementation notes

This issue may need a small product decision if the intended destination is ambiguous. Start by tracing the existing click handler and state model; if no intended behavior exists, implement the smallest behavior consistent with the current flow copy and the Group G baseline/what-if design.

## Blocked by

None - can start immediately.
