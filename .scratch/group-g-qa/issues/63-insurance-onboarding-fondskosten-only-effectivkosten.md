> *This was generated by AI during triage.*

title: "Insurance onboarding Fondskosten must not change Mantelgebühr"
Status: done
Severity: P1
Category: bug
Type: AFK
Area: onboarding / insurance products / fees

## What to build

Fix the insurance-product onboarding fee fields so editing `Fondskosten` affects only the all-in `Effektivkosten` calculation and does not mutate `Mantelgebühr`.

Today, changing `Fondskosten` in the onboarding flow for insurance-wrapper products appears to change the wrapper fee as well. That conflates fund TER with insurance-wrapper cost and makes the fee model misleading.

## Acceptance criteria

- [ ] In onboarding, editing `Fondskosten` for private insurance does not change `Mantelgebühr`.
- [ ] The same behavior is correct for other insurance-wrapper products that expose these fields, such as bAV and Basisrente, if they share the component.
- [ ] `Effektivkosten` updates from the intended all-in formula or field composition.
- [ ] Editing `Mantelgebühr` still changes the wrapper fee and updates `Effektivkosten`.
- [ ] Regression tests cover editing `Fondskosten` and `Mantelgebühr` independently.

## Implementation notes

Likely surfaces:

- `src/features/inventory/InstanceCard.tsx`
- `src/features/inputs/sections/FeeSection.tsx`
- product instance fee mapping in onboarding helpers

Keep the engine fee model split intact: `wrapperAssetFee` is the insurance wrapper fee, `fundAssetFee` is fund TER, and their sum can feed display-level `Effektivkosten`.

## Blocked by

None - can start immediately.
