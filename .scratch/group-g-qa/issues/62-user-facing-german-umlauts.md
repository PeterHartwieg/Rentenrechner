> *This was generated by AI during triage.*

title: "Use German umlauts in user-facing copy"
Status: done
Severity: P2
Category: bug
Type: AFK
Area: copy / localization / UI polish

## What to build

Audit user-facing German copy and replace ASCII transliterations like `ae`, `oe`, and `ue` with proper German umlauts where the text is visible to users.

German UI text should display `ä`, `ö`, `ü`, and `ß` where appropriate. ASCII transliterations are fine for filenames, internal identifiers, backend keys, URL-safe slugs, and code symbols, but visible copy without umlauts looks unfinished.

## Acceptance criteria

- [ ] User-facing labels, buttons, headings, helper text, chart copy, print/export visible headings, and legal/footer UI copy use proper German characters where appropriate.
- [ ] Internal identifiers, filenames, storage keys, test ids, route slugs, and backend/file-safe names remain ASCII unless there is a clear reason to change them.
- [ ] Existing tests are updated only where they assert user-visible copy.
- [ ] Add or update targeted tests for at least the most visible corrected labels.
- [ ] No mojibake is introduced; German characters render correctly in the browser.

## Implementation notes

Search for visible copy containing likely transliterations:

- `Luecke`, `schliessen`, `Beitraege`, `Schaetzung`, `fuer`, `ueber`, `koennen`, `waehlen`
- `Rentenbeginn` and other domain terms should be reviewed in context, not blindly replaced.

Do not rename files or internal code symbols just to add umlauts.

## Blocked by

None - can start immediately.
