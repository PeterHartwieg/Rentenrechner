> *This was generated by AI during triage.*

title: "Action buttons become too wide after Wunschnetto changes"
Status: done
Severity: P2
Category: bug
Type: AFK
Area: UI layout / recommender / responsive

## What to build

Fix the layout where `Beiträge anpassen` and `Lücke schließen` become very wide after the user changes the `Wunschnetto mtl.` input.

The action buttons should keep stable, professional dimensions regardless of the calculated target-gap state.

## Repro

1. Open the app in the new plan/recommender flow.
2. Change the `Wunschnetto mtl.` input.
3. Observe that the `Beiträge anpassen` and `Lücke schließen` buttons become excessively wide.

## Acceptance criteria

- [ ] Changing `Wunschnetto mtl.` does not cause either action button to expand beyond the intended action area.
- [ ] Button text remains readable and does not wrap awkwardly on desktop or mobile.
- [ ] The button group keeps stable spacing before and after recalculation.
- [ ] The fix works with the renamed `Mehr sparen` action from issue 64.
- [ ] Add a regression test or visual/layout-oriented component assertion where practical.

## Implementation notes

Likely cause is conditional content or flex/grid behavior after the target gap recalculates. Prefer stable container constraints over one-off text shortening.

## Blocked by

None - can start immediately.
