# Topic cluster — portfolio planner + Rente netto

Status: ready-for-agent
Type: AFK
Triaged: 2026-05-06 (decisions + agent brief in Comments)

## Parent

.scratch/seo-geo-discovery/PRD.md

## What to build

Add `/rente-netto-berechnen` (statutory pension net calculation: tax + KV + PV assumptions) and `/altersvorsorgeprodukte-vergleichen` (multi-product comparison framing with the "Wo geht mein nächster Euro hin?" recommender entry point) via the registry pattern.

## Acceptance criteria

- [ ] `/rente-netto-berechnen` registered with full metadata; covers statutory pension, retirement-tax pipeline (cohort Besteuerungsanteil, Versorgungsfreibetrag), KV/PV assumptions (KVdR vs freiwillig per §240 SGB V), Werbungskosten/Sonderausgaben Pauschbeträge.
- [ ] `/altersvorsorgeprodukte-vergleichen` registered with full metadata; explains the combine-mode comparison (per-instance arrays, transfer events, household totals) and links to the recommender entry point.
- [ ] Both pages prerender as static HTML.
- [ ] Each page links to homepage, calculator entry point, and at least two relevant siblings.
- [ ] `/altersvorsorgeprodukte-vergleichen` explicitly frames RentenWiki.de as a free model calculator, no broker/affiliate posture, no product recommendation, and routes the user into combine-mode rather than rendering simulator output statically.
- [ ] Content briefs at `.scratch/seo-geo-discovery/briefs/rente-netto-berechnen.md` and `altersvorsorgeprodukte-vergleichen.md`.
- [ ] FAQPage JSON-LD only where every Q/A is visibly rendered.
- [ ] Visible disclaimer + Stand 2026 on every page.
- [ ] `dist/sitemap.xml` includes both new canonical URLs.
- [ ] Unit tests verify registry entries, JSON-LD generation, disclaimer presence, internal-link counts.
- [ ] `npm run verify` passes.

## Blocked by

- `.scratch/seo-geo-discovery/issues/02-tracer-bullet-rentenluecke-rechner.md`

## Comments

### Agent Brief (2026-05-06)

> *This was generated by AI during triage.*

**Category:** enhancement
**Summary:** Ship the 2 topic pages in the portfolio + Rente-netto cluster via #02's registry + SSG pattern. `/rente-netto-berechnen` is calculator-shaped (`WebApplication`); `/altersvorsorgeprodukte-vergleichen` is the combine-mode portfolio framing (`Article`) with the "Wo geht mein nächster Euro hin?" recommender entry point.

**Current behavior (assumes #02, #03, #13 merged):**
SEO infrastructure in main. Homepage hub references both routes. `?topic=<slug>` preselection mechanism from #13 in main; combine-mode entries open the InventoryWizard with products preselected per #13's contract.

**Fallback if #13 has NOT landed when you start:** plain `/` CTAs, skip `preselection`. Sweep later. Check whether `PublicRoute` interface has the `preselection` field.

**Pages this slice ships:**

> **Note:** `'grv'` is NOT a `ProductId`. GRV is the implicit baseline that always renders alongside any compare-mode comparison. The `visibleProducts` arrays below contain only the explicit comparison-target products. For `/rente-netto-berechnen`, the natural seed is `['etf']` so the user sees statutory pension net (implicit GRV) compared against an ETF supplement — the most common framing of "Rentenlücke schließen". Adjust if the brief justifies a different seed.

| Route | Page shape | JSON-LD type | `preselection` (#13 contract) |
|-------|-----------|--------------|-----------------------------|
| `/rente-netto-berechnen` | Calculator (statutory pension, retirement-tax pipeline incl. cohort Besteuerungsanteil + Versorgungsfreibetrag, KV/PV via KVdR vs freiwillig per §240 SGB V, Werbungskosten/Sonderausgaben Pauschbeträge) | `WebApplication` + `BreadcrumbList` | `{ mode: 'compare', visibleProducts: ['etf'] }` |
| `/altersvorsorgeprodukte-vergleichen` | Portfolio framing (combine-mode, per-instance arrays, transfer events, household totals; routes the user into `Mein Plan` rather than rendering simulator output statically) | `Article` + `BreadcrumbList` | `{ mode: 'combine' }` (omit `visibleProducts`; user adds what they have via the wizard) |

**Decisions resolved during triage (apply across #04–#07):**

| # | Decision | Choice |
|---|----------|--------|
| 1 | JSON-LD type per page-shape | Calculator-shaped → `WebApplication`; Comparison/explanatory → `Article` |
| 2 | FAQPage scope | Optional per page; agent decides based on brief |
| 3 | BreadcrumbList | Always emit (Home → Topic) |
| 4 | Static example tables | Allowed with `Beispiel: Persona X (Stand 2026)` framing + persona reference from `docs/user-scenarios.md`. Hardcoded numbers; no engine import |

**Body-copy YMYL guardrails (same as #02):**
- Citations: BMF, DRV, BMAS, GKV-Spitzenverband for §240 SGB V, §-references to EStG (especially §19 Abs. 2 EStG Versorgungsfreibetrag, §22 Nr. 1 Satz 3 a EStG cohort), SGB V (KV), SGB VI (statutory pension formula), SGB XI (PV), Statistisches Bundesamt for demographic baselines.
- For `/altersvorsorgeprodukte-vergleichen`: explicitly frame RentenWiki.de as a free model calculator, **no broker / affiliate / product-recommendation posture**. The "next euro" framing must not become "we recommend X". Frame the recommender as cap-headroom-driven and Monte-Carlo-P10-risk-aware (see `src/app/recommender.ts` for the rules engine).
- Citations inline + `.scratch/seo-geo-discovery/briefs/<slug>.md`.
- "Stand 2026" visibly present; JSON-LD `dateModified` matches.
- No winner / recommendation framing.
- PR review blocks on weakly-sourced claims.

**Acceptance criteria (per page in the table above):**
- [ ] Registered in `publicRouteRegistry` with full metadata + `preselection` if #13 landed.
- [ ] Prerenders as static HTML.
- [ ] Route-specific head: `<title>`, meta description, `<link rel="canonical">`, OG/Twitter, JSON-LD (`WebApplication` or `Article` + `BreadcrumbList`; FAQPage only if 3+ visible Q/As).
- [ ] Visible H1, summary, Stand 2026, source-cited body, internal links, calculator CTA, disclaimer.
- [ ] `/altersvorsorgeprodukte-vergleichen` CTA opens combine mode (`/?topic=altersvorsorgeprodukte-vergleichen` if #13 landed; else plain `/`). Visible copy emphasises portfolio modelling, not product recommendation.
- [ ] Optional persona-sourced "Beispielrechnung" table.
- [ ] Content briefs at `.scratch/seo-geo-discovery/briefs/{rente-netto-berechnen,altersvorsorgeprodukte-vergleichen}.md`.
- [ ] No engine import; no `localStorage` at render.
- [ ] Unit tests: registry, JSON-LD, BreadcrumbList, disclaimer, internal links, sitemap.
- [ ] `dist/sitemap.xml` includes both new canonical URLs.
- [ ] `npm run verify` passes.

**Out of scope:**
- Other cluster pages (#04/#05/#06).
- Per-route OG cards (#08).
- Cloudflare Content Signals (#09).
- `llms.txt` regenerates automatically.
- Homepage hub anchors — #03 already shipped.
- Topic-preselection mechanism (#13 ships the contract; only consume it).
- GSC/BWT/IndexNow (#11).
- Yearly checklist (#12).
- Frontend analytics — forbidden by PRD lines 120 + 149.
- Permanently dismissable disclaimer — forbidden by PRD line 157.
- Inline simulator output / live recommender on the topic page — explicitly forbidden by PRD line 134; the page routes into the calculator, it does not run it.
