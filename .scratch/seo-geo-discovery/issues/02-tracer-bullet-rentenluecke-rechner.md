# Public route registry + sitemap + robots tracer bullet (Rentenlücke-Rechner)

Status: ready-for-agent
Type: AFK
Triaged: 2026-05-06 (decisions + agent brief in Comments)

## Parent

.scratch/seo-geo-discovery/PRD.md

## What to build

Build the SEO/GEO infrastructure end-to-end against one real topic page (`/rentenluecke-rechner`). After this slice lands, every later page is a registry edit + content brief + tests, with no infrastructure work.

The slice cuts through every layer:

- Typed `src/seo/publicRouteRegistry.ts` (canonical URL, title, meta description, H1, summary, dateModified, robots policy, JSON-LD type, related routes, calculator CTA path).
- `vite-react-ssg` + MDX integration so `/rentenluecke-rechner` ships as prerendered static HTML.
- Generated `dist/sitemap.xml` derived from the registry; `dist/robots.txt` applying the locked crawler policy from issue 01.
- One JSON-LD block (`WebApplication`) generated from the registry, parsed and visible-content-compliant.
- Open Graph + Twitter metadata.
- Visible not-advice disclaimer ("keine Steuer-, Rechts- oder Anlageberatung").
- Internal link from homepage; calculator deep-link CTA into the relevant compare/combine flow.
- Content brief at `.scratch/seo-geo-discovery/briefs/rentenluecke-rechner.md`.
- Tests for metadata-as-data, sitemap content, robots content, disclaimer presence, no-localStorage-dependency render, share-URL canonical strip.

## Acceptance criteria

- [ ] `src/seo/publicRouteRegistry.ts` exists and exports a typed record with at least `/` and `/rentenluecke-rechner` entries.
- [ ] `vite-react-ssg` (or equivalent chosen in issue 01) is wired into `vite.config.ts`; `npm run build` emits prerendered HTML for `/rentenluecke-rechner`.
- [ ] Built artifact contains `dist/sitemap.xml` with the canonical URL list derived from the registry; query URLs and share-state URLs are excluded.
- [ ] Built artifact contains `dist/robots.txt` with the sitemap reference and the locked allow/disallow list (Googlebot, Bingbot, Applebot, OAI-SearchBot, ChatGPT-User, PerplexityBot allowed; GPTBot, ClaudeBot, anthropic-ai, Google-Extended, CCBot disallowed).
- [ ] `/rentenluecke-rechner` renders a visible H1, page summary, calculator CTA deep-link, statutory-year context ("Stand 2026"), source/evidence notes, internal links to siblings, and the not-advice disclaimer.
- [ ] `/rentenluecke-rechner` emits route-specific `<title>`, meta description, `<link rel="canonical">`, Open Graph + Twitter metadata, and one JSON-LD block (`WebApplication`).
- [ ] Canonical URL strips share-state query parameters; sitemap URL list does not include share URLs.
- [ ] On hydration, when share-state is detected via `readUrlState()`, `<meta name="robots" content="noindex,follow">` is injected dynamically.
- [ ] Page content does not import simulation engine code; rendering does not depend on `localStorage`.
- [ ] Content brief at `.scratch/seo-geo-discovery/briefs/rentenluecke-rechner.md` captures target query cluster, user intent, calculator path, sources, must-include caveats, FAQ entries (visible-only), and final copy.
- [ ] Unit tests verify registry entries have title, meta description, canonical, H1, dateModified, robots policy.
- [ ] Unit tests verify `sitemap.xml` content and structure.
- [ ] Unit tests verify `robots.txt` content matches the locked crawler policy.
- [ ] Unit tests verify the disclaimer is present in rendered HTML.
- [ ] Unit tests verify JSON-LD parses as JSON and only references entities visible on the page.
- [ ] Cloudflare Pages SPA fallback (`404.html`) still works for `/rentenluecke-rechner` direct loads.
- [ ] `npm run verify` passes.

## Blocked by

- `.scratch/seo-geo-discovery/issues/01-lock-seo-scope-architecture-decisions.md`

## Comments

### Agent Brief (2026-05-06)

> *This was generated by AI during triage.*

**Category:** enhancement
**Summary:** Build the SEO/GEO infrastructure (typed public-route registry + SSG + sitemap + robots + JSON-LD + canonical + OG) end-to-end against `/rentenluecke-rechner` and a real `/404` page, so every subsequent topic page (#03–#07) is a registry-edit-only addition.

**Current behavior:**
RentenWiki.de is a Vite + React SPA. Routing is a minimal pathname-based router with three known paths (`/`, `/impressum`, `/datenschutz`). The HTML head is bare — only `<title>RentenWiki.de</title>`, favicon, and viewport. There is no `sitemap.xml`, no `robots.txt`, no JSON-LD, no canonical/OG/Twitter metadata, and no mechanism to emit route-specific static HTML. Cloudflare Pages SPA fallback works by copying `dist/index.html` → `dist/404.html` at build time.

**Desired behavior:**
A typed public-route registry drives every SEO surface. Build emits prerendered static HTML for every registered public route, plus a real Page-Not-Found page registered in the same registry. `/rentenluecke-rechner` is the first topic page: prerendered, route-specific metadata, one JSON-LD `WebApplication` block, OG/Twitter referencing a shared brand-only placeholder image, an internal link from the existing landing copy, a calculator CTA to `/`, source-cited German body copy with statutory-year ("Stand 2026") context, and a visible not-advice disclaimer. Sitemap lists only canonical paths from the registry. Robots applies the locked allow/disallow crawler list. Share-state URLs are excluded from canonical and sitemap; on hydration, when share state is detected, a `<meta name="robots" content="noindex,follow">` is injected dynamically.

**Key interfaces (new contracts this slice introduces):**

- `PublicRoute` — typed interface with `canonical`, `title`, `metaDescription`, `h1`, `summary`, `dateModified` (ISO), `robots` policy, `jsonLdType` (subset of schema.org types), `relatedRoutes` (sibling slugs), `calculatorCta` (`{ label, href }` — `href` is `/` for now; deep-link preselection deferred to #13).
- `publicRouteRegistry` — typed `Record` keyed by route path; `PublicRouteId = keyof typeof publicRouteRegistry` derived from registry keys (mirrors the `productRegistry.ts` ADR pattern).
- `<JsonLd type={...} data={...} />` — minimal React component (~5 lines) using `schema-dts` types; renders `<script type="application/ld+json">` inline so it lands in prerendered HTML.
- Sitemap and robots generators — pure functions over the registry; deterministic byte-for-byte output for the same input.
- Canonical-URL helper — strips query parameters; sitemap consumer uses only canonical paths.
- Client-side robots-meta injector — runs on hydration; if `readUrlState()` returns share state, dynamically inserts `<meta name="robots" content="noindex,follow">`.
- Static-rendering pipeline — `vite-react-ssg` (or equivalent contemporary package; agent picks based on Vite 7 ecosystem). Emits one HTML file per registered route + one for `/404`. The calculator at `/` continues to hydrate normally; the landing variant is what gets prerendered for first paint.

**Crawler policy (locked in #01):**
Allow `Googlebot`, `Bingbot`, `Applebot`, `OAI-SearchBot`, `ChatGPT-User`, `PerplexityBot`. Disallow `GPTBot`, `ClaudeBot`, `anthropic-ai`, `Google-Extended`, `CCBot`. Sitemap reference at the top of `robots.txt`. Cloudflare zone-level Content Signals are out of scope here — they ship in #09.

**Page-copy guardrails (PRD story 34 + line 168 — YMYL):**
- Agent may draft the German body copy for `/rentenluecke-rechner`.
- **Every substantive claim about German pension or tax mechanics must cite a public source.** Acceptable sources: BMF circulars; DRV publications; BMAS publications for §240 SGB V; §-references to EStG, SGB V, SGB VI, AltZertG, AltvVerbG; BaFin publications for insurance topics; Statistisches Bundesamt for demographic baselines.
- Citations live inline in the page body, and a structured citation list lives in the brief at `.scratch/seo-geo-discovery/briefs/rentenluecke-rechner.md`.
- "Stand 2026" must be visibly present; JSON-LD `dateModified` must match.
- The not-advice disclaimer must be present on the page and survive prerendering — covered by tests.
- No page-level "winner" or recommendation copy. Frame the topic as: what the gap is, how it is calculated, what inputs the calculator needs, what assumptions vary.
- The PR review verifies source accuracy; weakly-sourced or un-sourced claims block merge.

**Decisions resolved during triage:**

| # | Decision | Choice |
|---|----------|--------|
| 1 | Registry shape | Typed interface; `PublicRouteId` derived from registry keys (`productRegistry.ts` pattern) |
| 2 | Calculator CTA target | Plain link to `/`. `?topic=<slug>` preselection deferred to issue #13 |
| 3 | JSON-LD generation | `schema-dts` types + hand-rolled `<JsonLd>` component |
| 4 | MDX wiring | `.tsx` page wrapper imports `.mdx` body; registry registration stays in `.tsx` |
| 5 | Homepage edit in #02 | Single descriptive-anchor link from existing landing copy to `/rentenluecke-rechner`. Full hub for all 10 topic pages stays for #03 |
| 6 | OG image | Single brand-only placeholder at `/og/default.png` shared across all routes (per-route cards in #08) |
| 7 | SPA fallback | Real `/404` Page-Not-Found page registered in the route registry (replace the legacy `dist/404.html` = `dist/index.html` copy) |
| 8 | Copy authoring | Agent drafts with mandatory source citations; human reviews in PR |

**Acceptance criteria (supersedes the original list above):**

- [ ] Typed `publicRouteRegistry` exists with entries for `/`, `/rentenluecke-rechner`, and `/404`. `PublicRouteId` is derived from registry keys.
- [ ] `schema-dts` is added as a dependency; `<JsonLd>` component renders typed schema.org objects inline.
- [ ] `vite-react-ssg` (or chosen equivalent) is wired in. `npm run build` emits prerendered static HTML for each registered route plus `/404`.
- [ ] `dist/sitemap.xml` is generated from the registry; lists canonical URLs only; excludes query/share URLs.
- [ ] `dist/robots.txt` is generated, references the sitemap, applies the locked allow/disallow list (Googlebot, Bingbot, Applebot, OAI-SearchBot, ChatGPT-User, PerplexityBot allowed; GPTBot, ClaudeBot, anthropic-ai, Google-Extended, CCBot disallowed).
- [ ] `/rentenluecke-rechner` renders a visible H1, page summary, statutory-year ("Stand 2026") note, source-cited body copy, internal links to homepage and at least one sibling, calculator CTA to `/`, and the not-advice disclaimer.
- [ ] `/rentenluecke-rechner` emits route-specific `<title>`, meta description, `<link rel="canonical">`, OG meta (`og:image=/og/default.png`), Twitter meta, and a JSON-LD `WebApplication` block whose entities only reference visible page content.
- [ ] Canonical URL strips share-state query parameters.
- [ ] On hydration, when `readUrlState()` returns share state, `<meta name="robots" content="noindex,follow">` is injected dynamically.
- [ ] A new `/404` route is registered; its prerendered page renders the not-advice disclaimer and a link back to `/`. The legacy `404.html = index.html` copy step is replaced.
- [ ] `public/og/default.png` exists as a RentenWiki.de brand-only placeholder. Per-route cards remain out of scope (#08).
- [ ] The existing landing-page copy gains one descriptive-anchor internal link to `/rentenluecke-rechner`. The broader hub for all topic pages stays scoped to #03.
- [ ] Topic-page content does not import simulation engine code; rendering does not depend on `localStorage`.
- [ ] `.scratch/seo-geo-discovery/briefs/rentenluecke-rechner.md` exists and captures: target query cluster, user intent, calculator deep-link path, cited sources (BMF / DRV / §-refs), must-include caveats, final H1 + meta + body, FAQ entries (visible-only).
- [ ] Unit tests cover: registry entry shape; sitemap.xml structure + content; robots.txt content matches the locked policy; JSON-LD parses + entities are visible-content-only; disclaimer present in prerendered HTML; canonical strip works for share URLs; no `localStorage` access during render.
- [ ] Existing routes (`/`, `/impressum`, `/datenschutz`) continue to work; no regression to the in-app router or to the `DisclaimerBanner` session-only behavior.
- [ ] `npm run verify` passes.

**Out of scope:**

- Topic→preselection deep-link mechanism (`?topic=<slug>`) — issue #13.
- Per-route OG card images — issue #08. This slice ships one shared placeholder.
- Homepage internal-link hub for all 10 topic pages — issue #03. This slice adds only one link.
- Cloudflare zone-level Content Signals (`search=yes, ai-input=yes, ai-train=no`) — issue #09. The robots.txt allow/disallow rules ship here, but the CDN configuration does not.
- `llms.txt` / `llms-full.txt` — issue #10.
- All other topic pages (`/bav-rechner`, `/etf-vs-bav`, `/riester-rechner`, etc.) — issues #04–#07.
- GSC / Bing Webmaster / IndexNow / AI prompt-lab setup — issue #11.
- Yearly statutory-rule update checklist — issue #12.
- Frontend analytics, telemetry, cookies, error tracking, ad pixels — explicitly forbidden by PRD lines 120 + 149.
- Permanently dismissable disclaimer — explicitly forbidden by PRD line 157. The session-only `sessionStorage` behavior must remain.
