# Statutory rule-year update checklist for SEO content

Status: ready-for-agent
Triaged: 2026-05-06 (decisions + agent brief in Comments)
Type: AFK

## Parent

.scratch/seo-geo-discovery/PRD.md

## What to build

Extend the yearly `de2026.ts` → next-year migration checklist to include all SEO content freshness work: refresh "Stand 2026" copy on every public page, update `dateModified` in the registry, regenerate sitemap, audit JSON-LD `dateModified`, regenerate `llms.txt` + `llms-full.txt`, regenerate OG cards if they render the year, re-run AI prompt lab post-deploy.

## Acceptance criteria

- [ ] `docs/seo/yearly-update-checklist.md` (or extension to existing rules-update doc) lists every SEO surface that needs touching when the rule year moves: page Stand notes, registry `dateModified`, sitemap regeneration, JSON-LD `dateModified`, OG cards if year is rendered, `llms.txt` + `llms-full.txt` regeneration, post-deploy AI prompt-lab rerun.
- [ ] Checklist references the existing `src/rules/index.ts` swap-point so the SEO refresh is part of the same release train as the engine update.
- [ ] Checklist is linked from `CLAUDE.md` (Quick navigation table) or `BACKLOG.md` so future maintainers find it.
- [ ] Checklist includes a verification step: a smoke test or grep that no public page still says "Stand 2026" after the year flips.
- [ ] Checklist references the AI prompt lab from issue 11 as a post-deploy step.
- [ ] No code changes (documentation only).

## Blocked by

- `.scratch/seo-geo-discovery/issues/02-tracer-bullet-rentenluecke-rechner.md`

## Comments

### Agent Brief (2026-05-06)

> *This was generated by AI during triage.*

**Category:** enhancement
**State:** ready-for-agent

**Summary:** Write `docs/seo/yearly-update-checklist.md` and add a one-line link entry to `CLAUDE.md`'s Quick navigation table. Documentation only — no engine or frontend code changes.

**Current state (assumes #02, #10, #11 merged or known):**
- Rule-year swap point: `src/rules/index.ts` — change one re-export line to swap `de2026.ts` → `de2027.ts`. Comments in that file document the engine migration steps.
- SEO surfaces that carry the year: `dateModified` fields in `publicRouteRegistry.ts`; "Stand 2026" copy in topic-page MDX bodies; JSON-LD `dateModified`; `llms.txt` purpose statement (if it says "Stand 2026"); OG card if the year is rendered (issue #08 decision: OG cards show `h1` + brand — no year in the card, so no OG card update needed); sitemap `<lastmod>` (regenerated automatically from registry `dateModified`).
- `docs/seo/ai-prompt-lab.md` will exist after #11 lands.

**Decisions (locked):**

| Decision | Choice |
|----------|--------|
| Document location | New file `docs/seo/yearly-update-checklist.md` |
| CLAUDE.md link | Add one row to the Quick navigation table under "Update annual statutory values" — e.g. `\| SEO content freshness for rule-year update \| \`docs/seo/yearly-update-checklist.md\` \|` |
| OG card update | Note in checklist as "no action needed" — the OG card design (per #08 decision) renders H1 + brand wordmark, not the year. |
| Verification step | `grep -r "Stand 2026" src/features/publicPages/` — must return no matches after the year flips. Document this as the verification command. |

**`docs/seo/yearly-update-checklist.md` — required sections:**

1. **Overview** — when this applies (once per year, as part of the `de2026.ts` → `deYYYY.ts` migration), and what the release train looks like (engine + SEO + deploy).
2. **Engine update steps** — reference `src/rules/index.ts` swap-point (already documented in that file's comments); link to `src/rules/de2026.ts` as the template.
3. **SEO content steps (the new content this issue adds):**
   - [ ] Update `dateModified` in `publicRouteRegistry.ts` for every registered public route.
   - [ ] Search and replace "Stand 2026" → "Stand YYYY" in all topic-page MDX/TSX bodies under `src/features/publicPages/`.
   - [ ] Verify no "Stand 2026" references remain: `grep -r "Stand 2026" src/features/publicPages/` (must return empty).
   - [ ] Verify `dateModified` consistency: all in-sitemap routes should have the new year in `dateModified`. A grep check: `grep "dateModified" src/seo/publicRouteRegistry.ts | grep "2026"` (must return empty after update).
   - [ ] Run `npm run build` — regenerates sitemap, robots, llms.txt, llms-full.txt, OG cards, prerendered HTML with updated dates.
   - [ ] JSON-LD `dateModified` in rendered HTML — confirm via `dist/rentenluecke-rechner/index.html` spot-check.
   - [ ] OG cards: no action needed (year not rendered in card design).
   - [ ] llms.txt purpose statement: check `src/seo/llmsTxt.ts` header string for "Stand 2026" and update if present.
4. **Post-deploy validation:**
   - [ ] Re-run the AI prompt lab (`docs/seo/ai-prompt-lab.md`) against the deployed site — look for stale "Stand 2026" citations.
   - [ ] Google Search Console → URL inspection on homepage and one topic page — confirm `lastmod` is updated.
   - [ ] Bing Webmaster Tools → submit updated sitemap.
   - [ ] IndexNow: POST updated URLs per `docs/seo/measurement.md`.
5. **Checklist completion gate** — all checkboxes above must be ticked before the rule-year branch is merged to main.

**Acceptance criteria (restated):**
- [ ] `docs/seo/yearly-update-checklist.md` exists with all sections above.
- [ ] CLAUDE.md Quick navigation table has a link to the checklist.
- [ ] Checklist references `src/rules/index.ts` swap-point.
- [ ] Checklist includes the `grep -r "Stand XXXX"` verification step.
- [ ] Checklist references `docs/seo/ai-prompt-lab.md` as a post-deploy step.
- [ ] No engine or frontend code changes.
- [ ] `npm run verify` passes.

**Out of scope:**
- OG card generation (#08), llms.txt generation (#10), measurement docs (#11), Cloudflare (#09).
- Actually running the year-update migration (that's a separate annual task).
- Frontend analytics, cookies, telemetry — forbidden (PRD lines 120+149).
