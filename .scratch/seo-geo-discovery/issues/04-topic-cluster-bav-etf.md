# Topic cluster — bAV ↔ ETF comparison

Status: ready-for-agent
Type: AFK
Triaged: 2026-05-06 (decisions + agent brief in Comments)

## Parent

.scratch/seo-geo-discovery/PRD.md

## What to build

Add `/bav-rechner` and `/etf-vs-bav` as new topic pages via the registry pattern from issue 02. Each page has a content brief, an MDX page body with comparison table + plain-language caveats + visible FAQ (when used in JSON-LD), source notes, statutory-year context, internal links to siblings, calculator CTAs, and a visible disclaimer.

`/bav-rechner` covers Entgeltumwandlung mechanics, employer subsidy, §3 Nr. 63 / §1 SvEV caps, GRV reduction, retirement KV/PV. `/etf-vs-bav` compares both with the same net-cost basis (matches the engine's fair-comparison invariant).

## Acceptance criteria

- [ ] `/bav-rechner` registered in `publicRouteRegistry.ts` with full metadata.
- [ ] `/etf-vs-bav` registered in `publicRouteRegistry.ts` with full metadata.
- [ ] Both pages prerender as static HTML via vite-react-ssg.
- [ ] `/etf-vs-bav` contains a comparison table with plain-language caveats (income, employer subsidy, fees, tax mode, KVdR vs freiwillig); **no page-level "winner" copy**.
- [ ] Both pages link to homepage, calculator entry point, and at least two relevant sibling pages (descriptive anchors).
- [ ] Calculator CTA on `/bav-rechner` deep-links to compare-mode with bAV preselected; CTA on `/etf-vs-bav` deep-links to ETF + bAV both visible.
- [ ] Content briefs at `.scratch/seo-geo-discovery/briefs/bav-rechner.md` and `etf-vs-bav.md` capture target query cluster, user intent, sources, caveats, final copy.
- [ ] FAQPage JSON-LD only generated where every Q/A is visibly rendered on the page.
- [ ] Both pages contain visible disclaimer and statutory-year ("Stand 2026") context.
- [ ] `dist/sitemap.xml` includes both new canonical URLs.
- [ ] Unit tests verify registry entries, JSON-LD generation (visible-content match), disclaimer presence, internal-link counts.
- [ ] `npm run verify` passes.

## Blocked by

- `.scratch/seo-geo-discovery/issues/02-tracer-bullet-rentenluecke-rechner.md`

## Comments

### Agent Brief (2026-05-06)

> *This was generated by AI during triage.*

**Category:** enhancement
**Summary:** Ship the 2 topic pages in the bAV ↔ ETF cluster via #02's registry + SSG pattern. `/bav-rechner` is a calculator-shaped single-product page (`WebApplication`); `/etf-vs-bav` is an explanatory comparison page (`Article`).

**Current behavior (assumes #02, #03, #13 merged):**
SEO infrastructure from #02 (`publicRouteRegistry`, `<JsonLd>`, sitemap/robots/SSG, prerendered HTML pipeline) is in main. Homepage hub from #03 already references both routes (links currently 404 until this slice lands). `?topic=<slug>` preselection mechanism from #13 is in main; routes register `preselection`, CTAs use `/?topic=<slug>`.

**Fallback if #13 has NOT landed when you start:** use plain `/` CTAs and skip `preselection` registration. A follow-up sweep handles `preselection` after #13 lands. Check whether `PublicRoute` interface has the `preselection` field — if yes, #13 is in main; if no, fall back.

**Pages this slice ships:**

> **Note:** `'grv'` is NOT a `ProductId`. GRV is the implicit baseline that always renders alongside any compare-mode comparison. The `visibleProducts` arrays below contain only the explicit comparison-target products.

| Route | Page shape | JSON-LD type | `preselection` (#13 contract) |
|-------|-----------|--------------|-----------------------------|
| `/bav-rechner` | Calculator (Entgeltumwandlung, employer subsidy, §3 Nr. 63 / §1 SvEV caps, GRV reduction, retirement KV/PV) | `WebApplication` + `BreadcrumbList` | `{ mode: 'compare', visibleProducts: ['etf', 'bav'] }` |
| `/etf-vs-bav` | Comparison (same net-cost basis, conditions/tradeoffs, no winner copy) | `Article` + `BreadcrumbList` | `{ mode: 'compare', visibleProducts: ['etf', 'bav'] }` |

**Decisions resolved during triage (apply across #04–#07):**

| # | Decision | Choice |
|---|----------|--------|
| 1 | JSON-LD type per page-shape | Calculator-shaped → `WebApplication`; Comparison/explanatory → `Article` |
| 2 | FAQPage scope | Optional per page; you decide based on brief content (skip unless 3+ visible Q/As emerge naturally) |
| 3 | BreadcrumbList | Always emit (Home → Topic) on every topic page |
| 4 | Static example tables | Allowed with `Beispiel: Persona X (Stand 2026)` framing + persona reference from `docs/user-scenarios.md` (Anna, Karin, Lena, Markus). Hardcoded numbers; no engine import |

**Body-copy YMYL guardrails (same as #02):**
- Every substantive claim about German pension/tax mechanics must cite a public source: BMF circulars, DRV publications, BMAS for §240 SGB V, §-references to EStG / SGB V / SGB VI / AltZertG / AltvVerbG / BetrAVG, BaFin, Statistisches Bundesamt.
- Citations inline in prose AND in a structured list inside each brief at `.scratch/seo-geo-discovery/briefs/<slug>.md`.
- "Stand 2026" visibly present; JSON-LD `dateModified` matches.
- No winner / recommendation framing on the comparison page (`besser als`, `lohnt sich`, `empfohlen` forbidden). Frame conditions, assumptions, tradeoffs.
- PR review blocks on weakly-sourced claims.

**Acceptance criteria (per page in the table above):**
- [ ] Registered in `publicRouteRegistry` with full metadata (canonical, title, meta description, h1, summary, dateModified, robots, jsonLdType, relatedRoutes, calculatorCta, `preselection` if #13 landed).
- [ ] Prerenders as static HTML via `scripts/prerender.mjs`.
- [ ] Emits route-specific `<title>`, meta description, `<link rel="canonical">`, OG/Twitter (og:image=`/og/default.png`), and the JSON-LD blocks per the locked type (`WebApplication` or `Article`, plus `BreadcrumbList` always).
- [ ] FAQPage JSON-LD only if the page has a visibly-rendered FAQ section (3+ Q/As).
- [ ] Visible H1, page summary, "Stand 2026" context, source-cited German body copy with §-references, internal links to homepage + calculator + at least 2 siblings (descriptive anchors), calculator CTA, visible disclaimer.
- [ ] Optional static "Beispielrechnung" table; if present, persona-sourced and labelled.
- [ ] Content brief at `.scratch/seo-geo-discovery/briefs/<slug>.md` (target query cluster, intent, calculator path, sources cited, must-include caveats, FAQ entries, final copy).
- [ ] Page content does not import engine code; rendering does not depend on `localStorage`.
- [ ] Unit tests cover registry entry, JSON-LD generation (visible-content match), `BreadcrumbList`, disclaimer presence, internal-link counts, sitemap inclusion.
- [ ] `dist/sitemap.xml` includes both new canonical URLs.
- [ ] `npm run verify` passes.

**Out of scope:**
- Other cluster pages (#05/#06/#07 are siblings; do not add their pages here).
- Per-route OG card images (#08); use shared `/og/default.png`.
- Cloudflare Content Signals (#09).
- `llms.txt` regeneration is automatic from the registry; no extra action.
- Homepage hub anchors — #03 already shipped them; do NOT re-edit.
- Topic-preselection mechanism — #13 ships the contract; you only register `preselection` per the existing field.
- GSC/BWT/IndexNow setup (#11).
- Yearly checklist (#12).
- Frontend analytics, cookies, telemetry — forbidden by PRD lines 120 + 149.
- Permanently dismissable disclaimer — forbidden by PRD line 157.
