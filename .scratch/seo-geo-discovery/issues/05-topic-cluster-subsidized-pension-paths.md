# Topic cluster — subsidized pension paths (Riester + AVD)

Status: ready-for-agent
Type: AFK
Triaged: 2026-05-06 (decisions + agent brief in Comments)

## Parent

.scratch/seo-geo-discovery/PRD.md

## What to build

Add `/riester-rechner`, `/altersvorsorgedepot-rechner`, and `/riester-vs-altersvorsorgedepot` via the registry pattern. Each page covers the relevant subsidized-pension product or comparison: allowance (Zulagen), tax benefit (Sonderausgabenabzug §10a), payout effects, and transfer mechanics for `Riester → AVD`, with plain-language caveats and source notes.

## Acceptance criteria

- [ ] `/riester-rechner` registered with full metadata; visible Stand 2026, Zulagen, Sonderausgabenabzug §10a context.
- [ ] `/altersvorsorgedepot-rechner` registered with full metadata; visible Stand 2026 context, mentions the new product status and §22 Nr. 5 EStG payout taxation.
- [ ] `/riester-vs-altersvorsorgedepot` registered with full metadata; comparison table with caveats; transfer mechanics referenced where relevant. **No "winner" copy.**
- [ ] All three pages prerender as static HTML via vite-react-ssg.
- [ ] Each page links to homepage, calculator entry point, and at least two relevant siblings.
- [ ] Calculator CTAs deep-link to the right combine-mode product card or compare-mode product selection.
- [ ] Content briefs at `.scratch/seo-geo-discovery/briefs/riester-rechner.md`, `altersvorsorgedepot-rechner.md`, `riester-vs-altersvorsorgedepot.md`.
- [ ] FAQPage JSON-LD only where every Q/A is visibly rendered.
- [ ] Visible disclaimer on every page.
- [ ] `dist/sitemap.xml` includes all three new canonical URLs.
- [ ] Unit tests verify registry entries, JSON-LD generation, disclaimer presence, internal-link counts.
- [ ] `npm run verify` passes.

## Blocked by

- `.scratch/seo-geo-discovery/issues/02-tracer-bullet-rentenluecke-rechner.md`

## Comments

### Agent Brief (2026-05-06)

> *This was generated by AI during triage.*

**Category:** enhancement
**Summary:** Ship the 3 topic pages in the subsidized-pension-paths cluster via #02's registry + SSG pattern. Two calculator-shaped single-product pages (`WebApplication`) and one comparison page (`Article`).

**Current behavior (assumes #02, #03, #13 merged):**
SEO infrastructure from #02 in main. Homepage hub from #03 already references all three routes (links 404 until this slice lands). `?topic=<slug>` preselection mechanism from #13 is in main.

**Fallback if #13 has NOT landed when you start:** use plain `/` CTAs and skip `preselection` registration. A follow-up sweep handles `preselection` after #13 lands. Check whether `PublicRoute` interface has the `preselection` field.

**Pages this slice ships:**

> **Note:** `'grv'` is NOT a `ProductId`. GRV is the implicit baseline that always renders alongside any compare-mode comparison. The `visibleProducts` arrays below contain only the explicit comparison-target products.

| Route | Page shape | JSON-LD type | `preselection` (#13 contract) |
|-------|-----------|--------------|-----------------------------|
| `/riester-rechner` | Calculator (Zulagen, Sonderausgabenabzug §10a, payout effects) | `WebApplication` + `BreadcrumbList` | `{ mode: 'compare', visibleProducts: ['etf', 'riester'] }` |
| `/altersvorsorgedepot-rechner` | Calculator (new product, §22 Nr. 5 EStG payout) | `WebApplication` + `BreadcrumbList` | `{ mode: 'compare', visibleProducts: ['etf', 'altersvorsorgedepot'] }` |
| `/riester-vs-altersvorsorgedepot` | Comparison (transfer mechanics referenced where relevant; no winner copy) | `Article` + `BreadcrumbList` | `{ mode: 'compare', visibleProducts: ['riester', 'altersvorsorgedepot'] }` |

**Decisions resolved during triage (apply across #04–#07):**

| # | Decision | Choice |
|---|----------|--------|
| 1 | JSON-LD type per page-shape | Calculator-shaped → `WebApplication`; Comparison/explanatory → `Article` |
| 2 | FAQPage scope | Optional per page; you decide based on brief content (skip unless 3+ visible Q/As emerge naturally) |
| 3 | BreadcrumbList | Always emit (Home → Topic) on every topic page |
| 4 | Static example tables | Allowed with `Beispiel: Persona X (Stand 2026)` framing + persona reference from `docs/user-scenarios.md`. Hardcoded numbers; no engine import |

**Body-copy YMYL guardrails (same as #02):**
- Every substantive claim must cite a public source: BMF circulars, DRV publications, ZfA (Zentrale Zulagenstelle für Altersvermögen) for Riester Zulagen, BMAS, §-references to EStG / SGB V / SGB VI / AltZertG / AltvVerbG, BaFin (Riester certification), Statistisches Bundesamt.
- AVD (Altersvorsorgedepot) is the new product — make sure the legal framing is accurate per §22 Nr. 5 EStG (Schicht 2, Schicht-2-Auszahlung tax). Cite the relevant statutory text or BMF circular if available.
- Citations inline in prose AND in `.scratch/seo-geo-discovery/briefs/<slug>.md`.
- "Stand 2026" visibly present; JSON-LD `dateModified` matches.
- No winner copy on the comparison page. Frame conditions, assumptions, tradeoffs.
- PR review blocks on weakly-sourced claims.

**Acceptance criteria (per page in the table above):**
- [ ] Registered in `publicRouteRegistry` with full metadata (canonical, title, meta description, h1, summary, dateModified, robots, jsonLdType, relatedRoutes, calculatorCta, `preselection` if #13 landed).
- [ ] Prerenders as static HTML via `scripts/prerender.mjs`.
- [ ] Emits route-specific `<title>`, meta description, `<link rel="canonical">`, OG/Twitter (og:image=`/og/default.png`), and JSON-LD blocks per the locked type (`WebApplication` or `Article`, plus `BreadcrumbList` always).
- [ ] FAQPage JSON-LD only if the page has 3+ visibly-rendered Q/As.
- [ ] Visible H1, summary, "Stand 2026", source-cited German body, internal links (homepage + calculator + ≥2 siblings), calculator CTA, visible disclaimer.
- [ ] Optional persona-sourced "Beispielrechnung" table.
- [ ] Content briefs at `.scratch/seo-geo-discovery/briefs/{riester-rechner,altersvorsorgedepot-rechner,riester-vs-altersvorsorgedepot}.md`.
- [ ] No engine import; no `localStorage` dependency at render.
- [ ] Unit tests: registry, JSON-LD, BreadcrumbList, disclaimer, internal links, sitemap.
- [ ] `dist/sitemap.xml` includes all three new canonical URLs.
- [ ] `npm run verify` passes.

**Out of scope:**
- Other cluster pages (#04/#06/#07).
- Per-route OG cards (#08).
- Cloudflare Content Signals (#09).
- `llms.txt` regenerates automatically from registry.
- Homepage hub anchors — #03 already ships them; do NOT re-edit.
- Topic-preselection mechanism (#13 ships the contract; only consume it).
- GSC/BWT/IndexNow (#11).
- Yearly checklist (#12).
- Frontend analytics, cookies, telemetry — forbidden by PRD lines 120 + 149.
- Permanently dismissable disclaimer — forbidden by PRD line 157.
