# Topic cluster — Basisrente + private Rentenversicherung

Status: ready-for-agent
Type: AFK
Triaged: 2026-05-06 (decisions + agent brief in Comments)

## Parent

.scratch/seo-geo-discovery/PRD.md

## What to build

Add `/basisrente-rechner` (covering Rürup + tax-deduction effects + payout-mode constraints) and `/private-rentenversicherung-rechner` (covering Versicherungsmantel fees, tax modes by contract era, payout choice) via the registry pattern.

## Acceptance criteria

- [ ] `/basisrente-rechner` registered with full metadata; covers Sonderausgabenabzug §10 Abs. 3 EStG, retirement Besteuerungsanteil, payout-mode constraints (no Kapitalauszahlung — only Leibrente / Zeitrente).
- [ ] `/private-rentenversicherung-rechner` registered with full metadata; covers wrapper fee vs. fund fee split (`wrapperAssetFee` + `fundAssetFee`), tax mode by contract era (pre-2005 / Halbeinkünfte / Abgeltungsteuer auto-derived), Leibrente vs. Kapitalverzehr.
- [ ] Both pages prerender as static HTML.
- [ ] Each page links to homepage, calculator entry point, and at least two relevant siblings.
- [ ] Calculator CTAs deep-link to the right product card / preselection.
- [ ] Content briefs at `.scratch/seo-geo-discovery/briefs/basisrente-rechner.md` and `private-rentenversicherung-rechner.md`.
- [ ] FAQPage JSON-LD only where every Q/A is visibly rendered.
- [ ] Visible disclaimer + Stand 2026 on every page.
- [ ] `dist/sitemap.xml` includes both new canonical URLs.
- [ ] Unit tests verify registry entries, JSON-LD generation, disclaimer presence, internal-link counts.
- [ ] `npm run verify` passes.

## Blocked by

- `.scratch/seo-geo-discovery/issues/02-tracer-bullet-rentenluecke-rechner.md`

## Comments

### Agent Brief (2026-05-06)

> *This was generated by AI during triage.*

**Category:** enhancement
**Summary:** Ship the 2 topic pages in the Basisrente + private-Rentenversicherung cluster via #02's registry + SSG pattern. Both pages are calculator-shaped (`WebApplication`).

**Current behavior (assumes #02, #03, #13 merged):**
SEO infrastructure in main. Homepage hub references both routes. `?topic=<slug>` preselection mechanism from #13 in main.

**Fallback if #13 has NOT landed when you start:** plain `/` CTAs, skip `preselection`. Sweep later. Check whether `PublicRoute` interface has the `preselection` field.

**Pages this slice ships:**

> **Note:** `'grv'` is NOT a `ProductId`. GRV is the implicit baseline that always renders alongside any compare-mode comparison. The `visibleProducts` arrays below contain only the explicit comparison-target products.

| Route | Page shape | JSON-LD type | `preselection` (#13 contract) |
|-------|-----------|--------------|-----------------------------|
| `/basisrente-rechner` | Calculator (Sonderausgabenabzug §10 Abs. 3, Besteuerungsanteil cohort, payout-mode constraints — **no Kapitalauszahlung**, only Leibrente / Zeitrente) | `WebApplication` + `BreadcrumbList` | `{ mode: 'compare', visibleProducts: ['etf', 'basisrente'] }` |
| `/private-rentenversicherung-rechner` | Calculator (`wrapperAssetFee` + `fundAssetFee` split, contract-era tax modes pre-2005 / Halbeinkünfte / Abgeltungsteuer auto-derived, Leibrente vs Kapitalverzehr) | `WebApplication` + `BreadcrumbList` | `{ mode: 'compare', visibleProducts: ['etf', 'versicherung'] }` |

**Decisions resolved during triage (apply across #04–#07):**

| # | Decision | Choice |
|---|----------|--------|
| 1 | JSON-LD type per page-shape | Calculator-shaped → `WebApplication`; Comparison/explanatory → `Article` |
| 2 | FAQPage scope | Optional per page; agent decides based on brief |
| 3 | BreadcrumbList | Always emit (Home → Topic) |
| 4 | Static example tables | Allowed with `Beispiel: Persona X (Stand 2026)` framing + persona reference from `docs/user-scenarios.md`. Hardcoded numbers; no engine import |

**Body-copy YMYL guardrails (same as #02):**
- Citations: BMF, DRV, BMAS, §-references to EStG (especially §10 Abs. 3, §22 Nr. 1 Satz 3 a EStG for Ertragsanteil, §20 Abs. 1 Nr. 6 EStG for capital-gains tax on insurance), BaFin for insurance product mechanics, Statistisches Bundesamt.
- Make the contract-era tax mode auto-derivation explicit on `/private-rentenversicherung-rechner` — pre-2005, Halbeinkünfte, Abgeltungsteuer all need clear visual guidance.
- For `/basisrente-rechner`, make the no-Kapitalauszahlung legal constraint visibly explicit — this is a frequent user surprise.
- Citations inline + `.scratch/seo-geo-discovery/briefs/<slug>.md`.
- "Stand 2026" visibly present; JSON-LD `dateModified` matches.
- No winner / recommendation framing.
- PR review blocks on weakly-sourced claims.

**Acceptance criteria (per page in the table above):**
- [ ] Registered in `publicRouteRegistry` with full metadata + `preselection` if #13 landed.
- [ ] Prerenders as static HTML.
- [ ] Route-specific head: `<title>`, meta description, `<link rel="canonical">`, OG/Twitter, JSON-LD (`WebApplication` + `BreadcrumbList`; FAQPage only if 3+ visible Q/As).
- [ ] Visible H1, summary, Stand 2026, source-cited body, internal links, calculator CTA, disclaimer.
- [ ] Optional persona-sourced "Beispielrechnung" table.
- [ ] Content briefs at `.scratch/seo-geo-discovery/briefs/{basisrente-rechner,private-rentenversicherung-rechner}.md`.
- [ ] No engine import; no `localStorage` at render.
- [ ] Unit tests: registry, JSON-LD, BreadcrumbList, disclaimer, internal links, sitemap.
- [ ] `dist/sitemap.xml` includes both new canonical URLs.
- [ ] `npm run verify` passes.

**Out of scope:**
- Other cluster pages (#04/#05/#07).
- Per-route OG cards (#08).
- Cloudflare Content Signals (#09).
- `llms.txt` regenerates automatically.
- Homepage hub anchors — #03 already shipped.
- Topic-preselection mechanism (#13 ships the contract; only consume it).
- GSC/BWT/IndexNow (#11).
- Yearly checklist (#12).
- Frontend analytics — forbidden by PRD lines 120 + 149.
- Permanently dismissable disclaimer — forbidden by PRD line 157.
