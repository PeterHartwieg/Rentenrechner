{
  "name": "rentenwiki-simulate",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker that exposes POST /simulate — an authenticated HTTP facade over runComparison(). Sanctioned by ADR-0002.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.5",
    "wrangler": "^4.0.0"
  }
}
