# SPA fallback — handled via 404.html (see vite.config.ts copy-404 plugin).
# Cloudflare Pages serves dist/404.html for any unmatched path (status 404,
# but the client-side router in src/app/useRoute.ts renders the correct view).
# The old "/* /index.html 200" rule triggers CF error 10021 (infinite loop).
