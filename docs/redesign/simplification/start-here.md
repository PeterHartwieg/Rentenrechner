# Fresh-session instruction

Implement `docs/redesign/simplification/implementation-plan.md` end to end in this repository. Read the repository instructions and inspect `docs/redesign/simplification/reference/mockup.html` through computer use before editing. The plan and bundled references contain the prior design decisions and review feedback; no previous chat context is needed.

Use Astra only for UI work: visible components, copy, layout, interactions, accessibility, and responsive UX. Use Opus 5 through the Claude Code CLI for mechanical work: types/adapters, state and storage, migrations, selectors, routing plumbing, scenario/undo logic, automated tests, and mechanical integration. Verify and explicitly select the actual Opus 5 model; do not silently substitute another model or assume the generic opus alias is correct. Start with Opus establishing the shared state and result-readiness contract, then orchestrate the UI and mechanical packages according to the ownership/dependency plan. The main session coordinates interfaces, single-editor ownership of shared files, and acceptance; mixed tasks must be split by responsibility. Use real calculation results and existing persistence; do not ship the mockup's figures or temporary saving behavior.

Proceed through implementation, appropriate regression checks, and desktop/mobile computer-use verification. Perform the independent UI-only novice reviews described in the plan, keeping them separate from source-code review. Resolve material findings and document any unavailable reviewer or genuine blocker. Finish with a reviewable implementation and a concise handoff. Do not deploy, publish, or merge.
